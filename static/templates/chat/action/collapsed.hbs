{{#if withoutResources}}
    <div class="notification warning">{{localize "AVANT.Item.Ability.CraftedItem.CraftedWithoutResources"}}</div>
{{/if}}

{{#if description.full~}}
    <div class="description">{{{description.full}}}</div>
{{~else if description.preview~}}
    <div class="description" data-tooltip-class="avant">
        <a class="preview" data-action="expand-description" data-tooltip="{{localize "AVANT.Action.ExpandDescription"}}">{{description.preview}}</a>
        <div class="shadow"></div>
    </div>
{{~/if}}

{{#if (or selfEffect craftedItem)}}
    <div class="message-buttons">
        {{#if craftedItem}}
            <div class="crafted-item">
                {{{localize "AVANT.Item.Ability.CraftedItem.Crafted" item=craftedItem}}}
            </div>
        {{/if}}
        {{#if selfEffect}}
            <button type="button" data-action="apply-effect" data-targets="{{actor.uuid}}" data-visibility="owner">
                {{localize "AVANT.Action.ApplyEffect"}}
            </button>
        {{/if}}
    </div>
{{/if}}
