<div class="avant chat-card item-card" data-actor-id="{{actor.id}}" data-item-id="{{item.id}}" {{#if tokenId}}data-token-id="{{tokenId}}"{{/if}}>
    <header class="card-header flexrow">
        <img src="{{item.img}}" alt="{{item.name}}" />
        <h3>{{item.name}}</h3>
    </header>

    <div class="tags" data-tooltip-class="avant">
        {{#each data.traits}}
            <span class="tag" data-trait="{{this.name}}" data-tooltip="{{this.description}}">{{localize this.label}}</span>
        {{/each}}
    </div>

    <div class="card-content">
        {{{data.description.value}}}
    </div>
</div>
