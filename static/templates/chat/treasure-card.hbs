<div class="avant chat-card item-card" data-actor-id="{{actor.id}}" data-item-id="{{item.id}}"{{#if tokenId}} data-token-id="{{tokenId}}"{{/if}}>
    <header class="card-header flexrow">
        <img src="{{item.img}}" title="{{item.name}}" width="36" height="36" />
        <h3>{{item.name}}</h3>
    </header>

    <div class="card-content">
        {{{data.description.value}}}
    </div>

    {{#unless (eq item.system.stackGroup "coins")}}
        <footer{{#unless item.isIdentified}} data-visibility="gm"{{/unless}}>
            <span>{{localize "AVANT.Item.Physical.LevelLabel" level=item.level}}</span>
            <span>{{localize "AVANT.Item.Physical.PriceLabel" price=(coinLabel item.price)}}</span>
        </footer>
    {{/unless}}
</div>
