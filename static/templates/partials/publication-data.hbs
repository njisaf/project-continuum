<input type="text" name="{{path}}.title" class="title" value="{{data.title}}" placeholder={{localize "AVANT.Publication.Title"}} />
<div class="authors">
    <label for="{{options.id}}-publication-authors">{{localize "AVANT.Publication.Authors"}}</label>
    <input
        type="text"
        name="{{path}}.authors"
        id="{{options.id}}-publication-authors"
        value="{{data.authors}}"
    />
</div>
<div class="license">
    <label for="{{options.id}}-publication-license">{{localize "AVANT.Publication.License.Label"}}</label>
    <select name="{{path}}.license" id="{{options.id}}-publication-license">
        {{selectOptions publicationLicenses selected=data.license localize=true}}
    </select>
</div>
<div class="remaster">
    <label for="{{options.id}}-publication-remaster">{{localize "AVANT.Publication.Remaster"}}</label>
    <input
        type="checkbox"
        name="{{path}}.remaster"
        id="{{options.id}}-publication-remaster"
        {{checked data.remaster}}
    />
</div>
