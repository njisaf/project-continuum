<div>
    <section class="tab {{#if (eq tabGroups.primary "core")}}active{{/if}}" data-tab="core" data-group="primary">
        <div data-application-part="form"></div>
    </section>

    {{!-- Avant Settings --}}
    <section class="tab standard-form{{#if (eq tabGroups.primary "avant")}} active{{/if}}" data-tab="avant" data-group="primary">
        <div class="form-group">
            <label for="{{user.uuid}}-showEffectPanel">{{localize "AVANT.SETTINGS.User.EffectPanel.Name"}}</label>
            <div class="form-fields">
                <input id="{{user.uuid}}-showEffectPanel" type="checkbox" name="flags.avant.settings.showEffectPanel" {{checked user.flags.avant.settings.showEffectPanel}} />
            </div>
            <p class="hint">{{localize "AVANT.SETTINGS.User.EffectPanel.Hint"}}</p>
        </div>

        <div class="form-group">
            <label for="{{user.uuid}}-showCheckDialogs">{{localize "AVANT.SettingsQuickChecksLabel"}}</label>
            <div class="form-fields">
                <input id="{{user.uuid}}-showCheckDialogs" type="checkbox" name="flags.avant.settings.showCheckDialogs" {{checked user.flags.avant.settings.showCheckDialogs}} />
            </div>
            <p class="hint">{{localize "AVANT.SettingsQuickChecksNotes"}}</p>
        </div>

        <div class="form-group">
            <label for="{{user.uuid}}-showDamageDialogs">{{localize "AVANT.SettingsQuickDamageLabel"}}</label>
            <div class="form-fields">
                <input id="{{user.uuid}}-showDamageDialogs" type="checkbox" name="flags.avant.settings.showDamageDialogs" {{checked user.flags.avant.settings.showDamageDialogs}} />
            </div>
            <p class="hint">{{localize "AVANT.SettingsQuickDamageNotes"}}</p>
        </div>

        <div class="form-group monochrome-darkvision">
            <label for="{{user.uuid}}-monochromeDarkvision">{{localize "AVANT.SETTINGS.User.MonochromeDarkvision.Name"}}</label>
            <div class="form-fields">
                <input id="{{user.uuid}}-monochromeDarkvision" type="checkbox" name="flags.avant.settings.monochromeDarkvision" {{checked user.flags.avant.settings.monochromeDarkvision}} />
            </div>
            <p class="hint">{{localize "AVANT.SETTINGS.User.MonochromeDarkvision.Hint"}}</p>
        </div>

        <footer class="form-footer">
            <button type="submit">
                <i class="fa-solid fa-floppy-disk"></i>
                <label>{{localize "USER.SHEET.BUTTONS.SAVE"}}</label>
            </button>
        </footer>
    </section>
</div>
