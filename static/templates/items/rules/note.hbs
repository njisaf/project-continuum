<div class="form-group">
    <label class="short">{{localize "AVANT.RuleEditor.Note.Title"}}</label>
    <div class="form-fields">
        <input type="text" name="system.rules.{{index}}.title" value="{{rule.title}}" />
        <label>
            {{localize "AVANT.RuleEditor.Note.Hidden"}}
            <input type="checkbox" class="hidden-value" {{checked (or (eq rule.visibility "owner") (eq rule.visibility "gm"))}}>
        </label>
    </div>
</div>

<div class="form-group">
    <label for="selector" class="short">{{localize "AVANT.RuleEditor.General.Selector"}}</label>
    <div class="attached-input">
        <button type="button" class="attachment" data-action="toggle-selector">
            {{#if selectorIsArray}}Multiple{{else}}Single{{/if}}
        </button>
        {{#if selectorIsArray}}
            <tagify-tags class="avant-tagify selector-list" name="system.rules.{{index}}.selector" value="{{json rule.selector}}" />
        {{else}}
            <input type="text" name="system.rules.{{index}}.selector" value="{{rule.selector}}" />
        {{/if}}
    </div>
</div>

<textarea name="system.rules.{{index}}.text" spellcheck="false" placeholder="{{localize "AVANT.RuleEditor.Note.Text"}}">{{rule.text}}</textarea>

<div class="form-group">
    <label class="short">{{localize "AVANT.RuleEditor.Note.Outcome"}}</label>
    <tagify-tags class="avant-tagify outcomes" name="system.rules.{{index}}.outcome" value="{{json rule.outcome}}" />
</div>

<div class="form-group">
    <label class="short">{{localize "AVANT.RuleEditor.General.Predicate"}}</label>
    <input type="text" name="system.rules.{{index}}.predicate" value="{{json rule.predicate}}" />
</div>
