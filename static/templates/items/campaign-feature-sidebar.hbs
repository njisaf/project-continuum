<div class="inventory-details">
    <div class="form-group">
        <label>{{localize "AVANT.Item.CampaignFeature.CampaignLabel"}}</label>
        <select name="system.campaign" {{disabled document.parent}}>
            <option value="kingmaker">{{localize "AVANT.SETTINGS.CampaignType.Choices.kingmaker"}}</option>
        </select>
    </div>

    <div class="form-group">
        <label>{{localize "AVANT.Category"}}</label>
        <select name="system.category" {{disabled document.parent}}>
            {{selectOptions categories selected=data.category localize=true}}
        </select>
    </div>

    <span class="item-summary">{{localize "AVANT.FeatPrereqLabel"}}</span>
    <ol class="item-tags tags">
        {{#each data.prerequisites.value as |v k|}}
            <li class="tag tag_transparent {{k}}">{{v.value}}</li>
        {{/each}}
    </ol>
</div>
