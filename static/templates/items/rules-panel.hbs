{{#if ruleEditing}}
    <section class="rule-editing">
        <div class="editor-placeholder"></div>
        <div class="item-controls">
            <button type="button" data-action="close">{{localize "AVANT.Item.Rules.Close"}}</button>
            <button type="button" data-action="apply">{{localize "AVANT.Item.Rules.Apply"}}</button>
        </div>
    </section>
{{else}}
    <div class="rules">
        <div class="nerd-details">
            <div class="form-group">
                <label for="{{options.id}}-uuid">
                    <span>{{localize "AVANT.UUID.Label"}}</span>
                    <i class="fa-solid fa-circle-info" data-tooltip="AVANT.Item.Rules.Hint.UUID"></i>
                </label>
                <input id="{{options.id}}-uuid" disabled value="{{item.uuid}}" />
                <div class="item-controls">
                    <a data-clipboard="{{item.uuid}}"><i class="fa-solid fa-fw fa-clipboard"></i></a>
                </div>
            </div>
            {{#if item.sourceId}}
                <div class="form-group">
                    <label for="{{options.id}}-source-id">
                        <span>{{localize "AVANT.UUID.SourceId"}}</span>
                        <i class="fa-solid fa-circle-info" data-tooltip="AVANT.Item.Rules.Hint.SourceId"></i>
                    </label>
                    <input id="{{options.id}}-source-id" disabled value="{{item.sourceId}}" />
                    <div class="item-controls">
                        <a data-clipboard="{{item.sourceId}}"><i class="fa-solid fa-fw fa-clipboard"></i></a>
                    </div>
                </div>
            {{/if}}
            <div class="form-group">
                <label for="{{options.id}}-slug">
                    <span>{{localize "AVANT.Slug"}}</span>
                    <i class="fa-solid fa-circle-info" data-tooltip="AVANT.Item.Rules.Hint.Slug"></i>
                </label>
                <input id="{{options.id}}-slug" type="text" name="system.slug" value="{{item.system.slug}}" />
                <div class="item-controls">
                    <a data-action="regenerate-slug" data-tooltip="AVANT.Item.Rules.RegenerateSlug"><i class="fa-solid fa-fw fa-sync-alt"></i></a>
                </div>
            </div>
            <div class="form-group">
                <a data-action="view-roll-options" data-tooltip-class="avant roll-options" data-tooltip-direction="RIGHT">
                    {{localize "AVANT.Item.Rules.ViewRollOptions"}}
                    <i class="fa-solid fa-circle-info"></i>
                </a>
            </div>
        </div>
        <div class="rule-element-forms">
            {{#each rules.elements as |form|}}
                {{{form.template}}}
            {{/each}}
        </div>
        <div class="create-rule-element">
            <select data-action="select-rule-element">
                {{selectOptions rules.selection.types selected=rules.selection.selected}}
            </select>
            <a data-action="add-rule-element"><i class="fa-solid fa-plus"></i> {{localize "AVANT.Item.Rules.New"}}</a>
        </div>
    </div>
{{/if}}
