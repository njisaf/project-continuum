<div class="content">
    <h1>
        {{effect.name}}
        <a data-action="send-to-chat" data-tooltip="{{localize "AVANT.NPC.SendToChat"}}"><i class="fa-solid fa-fw fa-comment-alt" ></i></a>
        {{#if (or (ne effect.type "condition") (eq effect.slug "persistent-damage"))}}
            <a data-action="edit" data-tooltip="{{localize "AVANT.EditItemTitle"}}"><i class="fa-solid fa-fw fa-pencil" ></i></a>
        {{/if}}
    </h1>
    {{#if (or (eq effect.type "effect") effect.breakdown)}}
        <div class="tags">
            {{#if (and effect.system.duration.sustained (not effect.system.expired))}}
                <div class="tag tag_secondary">{{localize "AVANT.Item.Spell.Sustained.Label"}}</div>
            {{/if}}
            {{#unless effect.isIdentified}}
                <div class="tag tag_secondary">{{localize "AVANT.EffectPanel.Unidentified"}}</div>
            {{/unless}}
            {{#if effect.breakdown}}
                <div class="tag tag_secondary">{{effect.breakdown}}</div>
            {{/if}}
            {{#if remaining}}
                <div class="tag tag_secondary">{{remaining}}</div>
            {{/if}}
        </div>
    {{/if}}
    {{#if (eq effect.slug "persistent-damage")}}
        <a class="inline-check" data-action="recover-persistent-damage">
            <i class="fa-solid fa-dice-d20 icon"></i>
            <span class="label">{{localize "AVANT.Item.Condition.PersistentDamage.AssistedRecovery"}}</span>
        </a>
    {{/if}}
    <div class="instructions">
        {{#if (nor effect.isLocked effect.inMemoryOnly)}}
            {{#if (eq effect.badge.type "formula")}}
                <div>{{localize "AVANT.EffectPanel.RollDamageToolTip"}}</div>
            {{/if}}

            {{#if (eq effect.badge.type "counter")}}
                <div>{{localize "AVANT.EffectPanel.IncrementToolTip"}}</div>
                <div>{{localize "AVANT.EffectPanel.DecrementToolTip"}}</div>
            {{else}}
                <div>{{localize "AVANT.EffectPanel.RemoveToolTip"}}</div>
            {{/if}}
        {{/if}}
    </div>

    <div class="description">
        {{{description}}}
    </div>
</div>
