<form autocomplete="off" onsubmit="event.preventDefault();">
    <p class="instructions">{{localize instructions}}</p>
    {{#each settings as |setting|}}
        {{> systems/avant/templates/system/settings/basic-setting.hbs setting=setting}}
    {{/each}}

    <footer>
        <button type="submit">
            <i class="fa-regular fa-floppy-disk"></i> {{localize "SETTINGS.Save"}}
        </button>
        <button type="reset">
            <i class="fa-solid fa-arrow-rotate-left"></i> {{localize "AVANT.SETTINGS.ResetChanges"}}
        </button>
    </footer>
</form>
