<template data-partial="tab">
    <a class="item" data-tab="avant"><span class="action-glyph">1</span> {{localize "AVANT.Pathfinder"}}</a>
</template>

<template data-partial="panel">
    <section class="tab" data-tab="avant">
        <div class="form-group">
            <label for="{{scene.uuid}}-rules-based-vision">{{localize "AVANT.SETTINGS.Automation.RulesBasedVision.Name"}}</label>
            <div class="form-fields">
                <select id="{{scene.uuid}}-rules-based-vision" name="flags.avant.rulesBasedVision">
                    {{selectOptions rbvOptions selected=scene.flags.avant.rulesBasedVision}}
                </select>

                <button type="button" class="automation-settings" data-action="world-rbv-setting"
                    data-tooltip="{{localize "AVANT.SETTINGS.Automation.RulesBasedVision.ViewWorldSetting"}}"
                >
                    <i class="fa-solid fa-globe fa-fw"></i>
                </button>
            </div>
            <p class="hint">{{localize "AVANT.SETTINGS.Automation.RulesBasedVision.Hint"}}</p>
        </div>

        <div class="form-group">
            <label for="{{scene.uuid}}-hearing-range">{{localize "AVANT.Scene.HearingRange.Name"}}</label>
            <div class="form-fields">
                <input type="number" id="{{scene.uuid}}-hearing-range" name="flags.avant.hearingRange"
                    value="{{scene.flags.avant.hearingRange}}"
                    placeholder="{{localize "AVANT.Scene.HearingRange.Unlimited"}}"
                />
            </div>
            <p class="hint">{{localize "AVANT.Scene.HearingRange.Hint"}}</p>
            <p class="hint caveat">{{localize "AVANT.Scene.HearingRange.Caveat"}}</p>
        </div>

        <div class="form-group">
            <label for="{{scene.uuid}}-environment-types">{{localize "AVANT.Region.Environment.Type.Label"}}</label>
            <div class="form-fields">
                <tagify-tags class="avant-tagify tags" id="{{scene.uuid}}-environment-types" name="flags.avant.environmentTypes" value="{{json environmentTypes}}">
            </div>
            <p class="hint">{{localize "AVANT.Region.Environment.Type.SceneHint"}}</p>
        </div>
    </section>
</template>
