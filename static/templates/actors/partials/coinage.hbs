<div class="coinage">
    <div class="currency">
        <li class="label">{{localize "AVANT.StackGroupCoins"}}</li>
        {{#each totalCoinage as |value denomination|}}
            <li class="denomination {{denomination}}">
                <div class="currency-image" data-tooltip="{{localize value.label}}"></div>
                <span>{{value.value}}</span>
            </li>
        {{/each}}
        {{#if editable}}
            <li>
                <button type="button" data-action="add-coins" data-tooltip="{{localize "AVANT.AddCoinsTitle"}}">
                    <i class="fa-solid fa-plus fa-fw"></i>
                </button>
            </li>
            <li>
                <button type="button" data-action="remove-coins" data-tooltip="{{localize "AVANT.RemoveCoinsTitle"}}">
                    <i class="fa-solid fa-minus fa-fw"></i>
                </button>
            </li>
            {{#if (or isLoot (eq @root.document.type "character"))}}
                <li data-tooltip="{{localize "AVANT.SellAllTreasureTitle"}}">
                    <button type="button" data-action="sell-all-treasure">
                        <i class="fa-solid fa-coins fa-fw"></i>
                    </button>
                </li>
            {{/if}}
            {{#if canDistributeCoins}}
                <li>
                    <button type="button" data-action="distribute-coins" data-tooltip="{{localize "AVANT.Actor.Inventory.DistributeCoins"}}" {{disabled (not canDistributeCoins.enabled)}}>
                        <i class="fa-solid fa-share-all fa-fw"></i>
                    </button>
                </li>
            {{/if}}
        {{/if}}
    </div>
    <div class="wealth">
        <h3 class="item-name">
            <i class="fa-solid fa-coins fa-fw"></i>
            {{localize "AVANT.TotalCoinage"}}
            <span>{{totalCoinageGold}} {{localize "AVANT.CurrencyAbbreviations.gp"}}</span>
        </h3>
        <h3 class="item-name">
            <i class="fa-solid fa-scale-unbalanced fa-fw"></i>
            {{localize "AVANT.TotalWealth"}}
            <span>{{totalWealthGold}} {{localize "AVANT.CurrencyAbbreviations.gp"}}</span>
        </h3>
    </div>
</div>
