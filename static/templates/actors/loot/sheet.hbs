<form class="{{cssClass}}" autocomplete="off" data-tooltip-class="avant">
    <!-- SIDEBAR -->
    {{> "systems/avant/templates/actors/loot/sidebar.hbs"}}

    <section class="content">
        <!-- HEADER -->
        <div class="sheet-header">
            {{#if owner}}
                <h1 class="charname">
                    <input name="name" type="text" value="{{actor.name}}" placeholder="{{localize "AVANT.loot.LootNamePlaceholder"}}" />
                </h1>
            {{else}}
                <h1 class="charname">{{actor.name}}</h1>
            {{/if}}
            <section class="sheet-type">
                {{#if owner}}
                    <i
                        class="fa-solid fa-circle-info"
                        data-tooltip="<div>{{localize "AVANT.loot.LootLabel"}}: {{localize "AVANT.loot.LootDescription"}}</div><div>{{localize "AVANT.loot.MerchantLabel"}}: {{localize "AVANT.loot.MerchantDescription"}}</div>"
                        data-tooltip-direction="LEFT"
                    ></i>
                    <select name="system.lootSheetType" class="type">
                        {{selectOptions lootSheetTypeOptions selected=data.lootSheetType localize=true}}
                    </select>
                {{else}}
                    <span class="type">{{data.lootSheetType}}</span>
                {{/if}}
            </section>
        </div>

        <!-- BODY -->
        {{> "systems/avant/templates/actors/loot/inventory.hbs"}}
    </section>

</form>
