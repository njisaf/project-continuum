<form data-tooltip-class="avant">
    <header class="sheet-header">
        <h1>
            {{entry.name}}
            {{#with entry.flexibleAvailable as |available|}}
                <div class="flexible-available">
                    {{localize "AVANT.SpellFlexibleAvailable" value=available.value max=available.max}}
                </div>
            {{/with}}
        </h1>
        <p class="hint">
            {{#if entry.isFlexible}}
                {{localize "AVANT.Actor.Creature.SpellPreparation.HintFlexible"}}
            {{else}}
                {{localize "AVANT.Actor.Creature.SpellPreparation.Hint"}}
            {{/if}}
        </p>

        <div class="search">
            <input type="search" spellcheck="false" placeholder="{{localize "AVANT.Actor.Creature.SpellPreparation.Search"}}" />
        </div>
    </header>

    <section class="sheet-content">
        <ol class="spell-list{{#if entry.isFlexible}} flexible{{/if}}" data-entry-id="{{entry.id}}">
            {{#unless entry.prepList}}
                {{#if @root.editable}}
                    <li class="spellbook-header spellbook-empty">
                        <h4>{{localize "AVANT.SpellSlotEmpty"}}</h4>
                        <a
                            data-action="create-spell"
                            data-tooltip="AVANT.CreateSpellTitle"
                            data-type="spell"
                            data-location="{{entry.id}}"
                        ><i class="fa-solid fa-plus fa-fw"></i>{{localize "AVANT.AddSpellTitle"}}</a>
                        <a
                            data-action="browse-spells"
                            data-tooltip="AVANT.OpenSpellBrowserTitle"
                            data-location="{{entry.id}}"
                        ><i class="fa-solid fa-magnifying-glass fa-fw"></i>{{localize "AVANT.OpenSpellBrowserTitle"}}</a>
                    </li>
                {{/if}}
            {{/unless}}
            {{#each entry.prepList as |spells rank|}}
                {{#if (lte rank @root.maxRank)}}
                    <li class="header-row">
                        <div class="item-name">
                            {{#if (gt rank 0)}}
                                {{localize "AVANT.Item.Spell.Rank.Ordinal" rank=(ordinal rank)}}
                            {{else}}
                                {{localize "AVANT.Actor.Creature.Spellcasting.Cantrips"}}
                            {{/if}}
                        </div>

                        <div class="defense">{{localize "AVANT.Item.Spell.Defense.Label"}}</div>

                        <div class="range">{{localize "AVANT.TraitRange"}}</div>

                        <div class="item-controls">
                            {{#if @root.editable}}
                                <a
                                    data-action="create-spell"
                                    data-tooltip="AVANT.CreateSpellTitle"
                                    data-type="spell"
                                    data-group-id="{{#if (eq rank "0")}}cantrips{{else}}{{rank}}{{/if}}"
                                    data-location="{{entry.id}}"
                                ><i class="fa-solid fa-plus fa-fw"></i></a>
                                <a
                                    data-action="browse-spells"
                                    data-tooltip="AVANT.OpenSpellBrowserTitle"
                                    data-category="{{#if (eq rank "0")}}cantrip{{else}}spell{{/if}}"
                                    data-rank="{{#if (eq rank "0")}}1{{else}}{{rank}}{{/if}}"
                                    data-location="{{entry.id}}"
                                ><i class="fa-solid fa-magnifying-glass fa-fw"></i></a>
                            {{/if}}
                        </div>
                    </li>

                    {{!-- Unprepared Spells --}}
                    {{#each spells as |item i|}}
                        <li class="item spell spellbook-item" data-item-id="{{item.spell.id}}" data-item-type="spell" draggable="true">
                            <div class="item-name">
                                <a class="item-image" data-action="spell-to-chat">
                                    <img class="item-icon" src="{{spell.img}}" />
                                    <i class="fa-solid fa-message"></i>
                                </a>
                                <h4 class="name">
                                    <a data-action="toggle-summary">{{spell.name}}</a>
                                    {{{actionGlyph spell.system.time.value}}}
                                </h4>
                            </div>

                            <div class="defense">{{spell.defense.label}}</div>

                            <div class="range">{{spell.system.range.value}}</div>

                            <div class="item-controls">
                                {{#if (and @root.entry.isFlexible (not spell.isCantrip))}}
                                    <input
                                        type="checkbox"
                                        data-action="toggle-flexible-collection"
                                        {{checked signature}}
                                        data-tooltip="{{#if signature}}AVANT.SpellCollectionRemove{{else}}AVANT.SpellCollectionAdd{{/if}}"
                                    />
                                {{/if}}

                                {{#if @root.editable}}
                                    <a data-action="edit-spell" data-tooltip="AVANT.EditItemTitle"><i class="fa-solid fa-edit fa-fw"></i></a>
                                    <a data-action="delete-spell" data-tooltip="AVANT.DeleteItemTitle"><i class="fa-solid fa-trash fa-fw"></i></a>
                                {{/if}}
                            </div>

                            <div class="item-summary" hidden></div>
                        </li>
                    {{/each}}
                {{/if}}
            {{/each}}
        </ol>
    </section>
</form>
