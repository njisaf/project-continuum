<div class="chat-card avant kingdom">
    <header class="card-header flexrow">
        <h3>{{localize "AVANT.Kingmaker.Kingdom.CollectDialog.Header"}}</h3>
    </header>
    <p>{{localize "AVANT.Kingmaker.Kingdom.CollectDialog.Description" dice=formula}}</p>
    <ul>
    {{#each commodities as |value slug|}}
        <li>{{value}} {{localize (concat "AVANT.Kingmaker.Kingdom.Commodity." slug)}}</li>
    {{/each}}
    </ul>
    <p>{{localize "AVANT.Kingmaker.Kingdom.CollectDialog.Disclaimer"}}</p>
    <button type="button" data-action="kingdom-collect" data-visibility="owner">
        {{localize "AVANT.Kingmaker.Confirm"}}
    </button>
</div>
