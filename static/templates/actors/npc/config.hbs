<form autocomplete="off">
    <div class="form-group">
        <label>{{localize "AVANT.Actor.Creature.Configure.Alliance.Label"}}</label>
        <select name="system.details.alliance">
            {{#each alliances as |option|}}
                <option value="{{option.value}}"{{#if option.selected}} selected="selected"{{/if}}>{{localize option.label}}</option>
            {{/each}}
        </select>
        <p class="hint">{{localize "AVANT.Actor.Creature.Configure.Alliance.Hint"}}</p>
    </div>
    <div class="form-group">
        <label>{{localize "AVANT.Actor.NPC.Configure.Lootable.Label"}}</label>
        <select name="flags.avant.lootable">
            {{#each lootable as |option|}}
                <option value="{{option.value}}"{{#if option.selected}} selected="selected"{{/if}}>{{localize option.label}}</option>
            {{/each}}
        </select>
        <p class="hint">{{{localize "AVANT.Actor.NPC.Configure.Lootable.Hint"}}}</p>
    </div>
    <button type="submit">
        <i class="fa-solid fa-floppy-disk"></i> {{localize "SETTINGS.Save"}}
    </button>
</form>
