<header>
  <h2>
      {{localize "AVANT.HitPointsHeader"}}
  </h2>
</header>
<div class="hitpoints">
  <div class="hp-big">
    <div class="container temp-hp" title="{{localize "AVANT.ShieldBrokenThresholdLabel"}}">
      <span for="data.attributes.hp.brokenThreshold" class="sidebar_label">{{localize "AVANT.vehicle.BrokenThresholdShortLabel"}}</span>
      <div class="data-value">
        <h2>{{data.attributes.hp.brokenThreshold}}</h2>
      </div>
    </div>
    <div class="container current-hp" title="{{localize "AVANT.vehicle.CurrentHPTitle"}}">
      <label for="{{options.id}}-vehicle-hp-temp" class="sidebar_label">{{localize "AVANT.CurrentHitPointsShortLabel"}}</label>
      <div class="data-value">
        <input id="{{options.id}}-vehicle-hp-temp" name="system.attributes.hp.value" type="number" value="{{data.attributes.hp.value}}" data-dtype="Number" placeholder="0" size="1"/>
      </div>
    </div>
    <div class="container max-hp" title="{{localize "AVANT.vehicle.MaxHPTitle"}}">
      <label for="{{options.id}}-vehicle-hp-max" class="sidebar_label">{{localize "AVANT.MaxHitPointsShortLabel"}}</label>
      <div class="data-value">
        <input id="{{options.id}}-vehicle-hp-max" data-property="system.attributes.hp.max" type="number" value="{{data.attributes.hp.max}}" placeholder="0" size="1" />
      </div>
    </div>
  </div>
</div>

<header>
    <h2>{{localize "AVANT.ArmorClassLabel"}}</h2>
</header>
<div class="armor-class">
    <div class="shield ac" title="{{localize "AVANT.ArmorClassLabel"}}">
        <div class="data-value">
            <input id="{{options.id}}-vehicle-ac" class="adjustable {{ac.adjustmentClass}}" data-property="system.attributes.ac.value" type="number" value="{{ac.value}}" data-dtype="Number" placeholder="10" size="1"/>
        </div>
        <label for="{{options.id}}-vehicle-ac" class="sidebar_label">{{localize "AVANT.ArmorClassShortLabel"}}</label>
    </div>
    <div class="shield hardness" title="{{localize "AVANT.vehicle.HardnessLabel"}}">
        <div class="data-value">
            <input
                id="{{options.id}}-vehicle-hardness"
                type="number"
                data-dtype="Number"
                placeholder="0"
                data-property="system.attributes.hardness"
                value="{{data.attributes.hardness}}"
            />
        </div>
        <label for="{{options.id}}-vehicle-hardness" class="sidebar_label">{{localize "AVANT.vehicle.HardnessShortLabel"}}</label>
    </div>
</div>

<header>
    <h2>{{localize "AVANT.SavesHeader"}}</h2>
</header>
<ul class="sidebar-saves">
    <li class="roll-data" data-save="fortitude">
        <h2 class="sidebar_label">{{localize "AVANT.SavesFortitude"}}</h2>
        <div class="save-roll">
            {{#if options.editable}}
                <a class="roll-icon" data-action="roll-check" data-statistic="fortitude">
                    {{> "systems/avant/templates/actors/character/icons/d20.hbs"}}
                </a>
            {{/if}}
            <input class="adjustable {{saves.fortitude.adjustmentClass}}" data-property="system.saves.fortitude.value" type="number" value="{{saves.fortitude.value}}" placeholder="0" />
        </div>
    </li>
</ul>

<header>
    <h2>{{localize "AVANT.ImmunitiesLabel"}}</h2>
    {{#if editable}}
        <button type="button" class="modifiers_button" data-action="edit-immunities">
            <i class="fa-solid fa-edit"></i> {{localize "AVANT.Edit"}}
        </a>
    {{/if}}
</header>
<ol class="immunities tags">
    {{#each data.attributes.immunities as |immunity|}}
        <li class="tag tag_secondary" data-immunity="{{immunity.type}}">{{immunity.label}}</li>
    {{/each}}
</ol>

<header>
    <h2>{{localize "AVANT.WeaknessesLabel"}}</h2>
    {{#if editable}}
        <button type="button" class="modifiers_button" data-action="edit-weaknesses">
            <i class="fa-solid fa-edit"></i> {{localize "AVANT.Edit"}}
        </button>
    {{/if}}
</header>
<ol class="weaknesses tags">
    {{#each data.attributes.weaknesses as |weakness|}}
        <li class="tag tag_secondary" data-weakness="{{weakness.type}}">{{weakness.label}}</li>
    {{/each}}
</ol>

<header>
    <h2>{{localize "AVANT.ResistancesLabel"}}</h2>
    {{#if editable}}
        <button type="button" class="modifiers_button" data-action="edit-resistances">
            <i class="fa-solid fa-edit"></i> {{localize "AVANT.Edit"}}
        </button>
    {{/if}}
</header>
<ol class="resistances tags">
    {{#each data.attributes.resistances as |resistance|}}
        <li class="tag tag_secondary" data-resistance="{{resistance.type}}">{{resistance.label}}</li>
    {{/each}}
</ol>
<section class="emits-sound">
    <header class="sound-label">
        <h2>
            <span>{{localize "AVANT.Actor.Vehicle.EmitsSound.Title"}}</span>
            <i
                class="fa-solid fa-circle-info"
                data-tooltip="AVANT.Actor.Vehicle.EmitsSound.Hint"
                data-tooltip-direction="RIGHT"
            ></i>
        </h2>
    </header>
    <select name="system.attributes.emitsSound">
        {{selectOptions emitsSoundOptions selected=data.attributes.emitsSound localize=true}}
    </select>
</section>
